{
  "_comment": "Merge this into ~/.claude/settings.json (global) or .claude/settings.json (per-project). If a 'hooks' key already exists, add the UserPromptSubmit entry to it rather than overwriting. Replace the path if you put ship-bar.sh somewhere else. The script must be executable: chmod +x ship-bar.sh",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude/hooks/agent-work-ethic/ship-bar.sh"
          }
        ]
      }
    ]
  }
}
